syntax = "proto3";

package addrsvc;
option go_package="./addrsvc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Addrsvc {
  rpc Ping(Request) returns(Response);
}
