syntax = "proto3";

package queuesvc;
option go_package="./queuesvc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Queuesvc {
  rpc SendQueue(Request) returns(Response);
  rpc ReadQueue(Request) returns(Response);
}
