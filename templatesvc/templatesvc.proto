syntax = "proto3";

package templatesvc;
option go_package="./templatesvc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Templatesvc {
  rpc Ping(Request) returns(Response);
}
